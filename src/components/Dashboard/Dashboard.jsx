import { ContentBlock, ContentContainer } from "common/GiobalStyles";


export default function Dashboard() {  

  return (
    <ContentContainer>
      <ContentBlock>
        Dashboard
      </ContentBlock>
    </ContentContainer>
  );
}