import { ContentBlock, ContentContainer } from "common/GiobalStyles";

export default function Customers() {  

  return (
    <ContentContainer>
      <ContentBlock>
        Customers
      </ContentBlock>
    </ContentContainer>
  );
}