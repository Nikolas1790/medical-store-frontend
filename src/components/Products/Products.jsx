import { ContentBlock, ContentContainer } from "common/GiobalStyles";

export default function Products() {  

  return (
    <ContentContainer>
      <ContentBlock>
        Products
      </ContentBlock>
    </ContentContainer>
  );
}