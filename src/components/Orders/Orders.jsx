import { ContentBlock, ContentContainer } from "common/GiobalStyles";

export default function Orders() {  

  return (    
    <ContentContainer>
      <ContentBlock>
        <p>Orders</p>
      </ContentBlock>
    </ContentContainer>
  );
}