import { ContentBlock, ContentContainer } from "common/GiobalStyles";

export default function Suppliers() {  

  return (
    <ContentContainer>
      <ContentBlock>
        Supplierss
      </ContentBlock>
    </ContentContainer>
  );
}